{
  "name": "sketch-manager",
  "version": "0.0.1",
  "repository": "github:alperr/sm",
  "dependencies": {
    "http": "github:nlibs/http"
  },
  "scripts": {
    "pub": "cp app.js ./bin/sm && git add -A && git commit -m 'npm release' && git push && npm publish"
  }
}
